if (${IOPSYS} STREQUAL "OS_Win32")
	message("Win32 source")
	file(GLOB WIN32_SRC "win32/*.h" "win32/*.cpp")
	set(Sigma_SRC ${Sigma_SRC} ${WIN32_SRC} PARENT_SCOPE)
	set(Sigma_LIB ${Sigma_LIB} soil.lib PARENT_SCOPE)
ELSEIF (${IOPSYS} STREQUAL "OS_SDL")
	message("SDL source")
	file(GLOB SDL_SRC "sdl/*.h" "sdl/*.cpp")
	set(Sigma_SRC ${Sigma_SRC} ${SDL_SRC} PARENT_SCOPE)
	if(WIN32)
	  set(Sigma_LIB ${Sigma_LIB} SDL2.lib soil.lib SDL2main.lib PARENT_SCOPE)
	endif(WIN32)
ENDIF (${IOPSYS} STREQUAL "OS_Win32")
